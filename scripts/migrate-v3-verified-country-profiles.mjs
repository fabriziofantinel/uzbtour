import{readFile}from"node:fs/promises";import{createHash}from"node:crypto";import{Client}from"@neondatabase/serverless";
const apply=process.argv.includes("--apply"),url=process.env.DATABASE_MIGRATION_URL??process.env.DATABASE_URL_UNPOOLED;if(!url)throw new Error("DATABASE_MIGRATION_URL non configurata");
const name="127_v3_verified_country_profiles",model="3.72.0-verified-country-profiles",source=await readFile(new URL(`../database/migrations/${name}.sql`,import.meta.url),"utf8"),checksum=createHash("sha256").update(source).digest("hex"),client=new Client(url);let open=false;
try{await client.connect();await client.query("BEGIN");open=true;await client.query("SET LOCAL lock_timeout='5s'");await client.query(source);const gate=(await client.query(`SELECT to_regclass('ref.country_verified_profiles') IS NOT NULL profiles,to_regclass('ref.country_verified_profile_revisions') IS NOT NULL revisions,has_function_privilege('smf_app','app.read_verified_country_profile_v3(uuid,uuid,uuid)','EXECUTE') read_ready,has_function_privilege('smf_app','app.save_country_profile_candidate_v3(uuid,uuid,uuid,jsonb,jsonb,jsonb,text,text,timestamptz)','EXECUTE') save_ready,has_function_privilege('smf_app','app.review_country_profile_v3(text,uuid,boolean)','EXECUTE') review_ready,has_function_privilege('smf_app','app.read_country_profiles_for_review_v3(text)','EXECUTE') review_list_ready`)).rows[0];if(!gate||Object.values(gate).some(v=>v!==true))throw new Error(`Gate incompleti: ${JSON.stringify(gate)}`);if(apply){await client.query(`INSERT INTO ops.schema_migrations(version,checksum_sha256)VALUES($1,$2)ON CONFLICT(version)DO UPDATE SET applied_at=clock_timestamp()`,[model,checksum]);await client.query("COMMIT");}else await client.query("ROLLBACK");open=false;console.log(JSON.stringify({status:apply?"applied":"dry_run_passed",gate}));}catch(error){if(open)await client.query("ROLLBACK").catch(()=>{});throw error;}finally{await client.end().catch(()=>{});}
